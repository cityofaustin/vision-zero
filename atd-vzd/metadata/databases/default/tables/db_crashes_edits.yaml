table:
  name: crashes_edits
  schema: db
object_relationships:
  - name: crashes_cri
    using:
      foreign_key_constraint_on: crash_id
array_relationships:
  - name: change_log_crashes_edits
    using:
      foreign_key_constraint_on:
        column: record_id
        table:
          name: change_log_crashes_edits
          schema: db
