table:
  name: intrsct_relat_lkp
  schema: lookups
array_relationships:
  - name: crashes_cris
    using:
      foreign_key_constraint_on:
        column: intrsct_relat_id
        table:
          name: crashes_cris
          schema: db
  - name: crashes_edits
    using:
      foreign_key_constraint_on:
        column: intrsct_relat_id
        table:
          name: crashes_edits
          schema: db
  - name: crashes_unifieds
    using:
      foreign_key_constraint_on:
        column: intrsct_relat_id
        table:
          name: crashes_unified
          schema: db
