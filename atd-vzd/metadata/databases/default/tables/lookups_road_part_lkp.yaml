table:
  name: road_part_lkp
  schema: lookups
array_relationships:
  - name: crashesCrisByRptSecRoadPartId
    using:
      foreign_key_constraint_on:
        column: rpt_sec_road_part_id
        table:
          name: crashes_cris
          schema: db
  - name: crashesEditsByRptSecRoadPartId
    using:
      foreign_key_constraint_on:
        column: rpt_sec_road_part_id
        table:
          name: crashes_edits
          schema: db
  - name: crashesUnifiedsByRptSecRoadPartId
    using:
      foreign_key_constraint_on:
        column: rpt_sec_road_part_id
        table:
          name: crashes_unified
          schema: db
  - name: crashes_cris
    using:
      foreign_key_constraint_on:
        column: rpt_road_part_id
        table:
          name: crashes_cris
          schema: db
  - name: crashes_edits
    using:
      foreign_key_constraint_on:
        column: rpt_road_part_id
        table:
          name: crashes_edits
          schema: db
  - name: crashes_unifieds
    using:
      foreign_key_constraint_on:
        column: rpt_road_part_id
        table:
          name: crashes_unified
          schema: db
