table:
  name: atd_txdot__trvl_dir_lkp
  schema: public
select_permissions:
  - role: editor
    permission:
      columns:
        - trvl_dir_id
        - trvl_dir_desc
        - eff_beg_date
        - eff_end_date
      filter: {}
  - role: readonly
    permission:
      columns:
        - trvl_dir_id
        - eff_beg_date
        - eff_end_date
        - trvl_dir_desc
      filter: {}
  - role: vz-admin
    permission:
      columns:
        - trvl_dir_id
        - trvl_dir_desc
        - eff_beg_date
        - eff_end_date
      filter: {}
