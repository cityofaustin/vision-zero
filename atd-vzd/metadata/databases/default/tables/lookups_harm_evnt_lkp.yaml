table:
  name: harm_evnt_lkp
  schema: lookups
array_relationships:
  - name: units_cris
    using:
      foreign_key_constraint_on:
        column: first_harm_evt_inv_id
        table:
          name: units_cris
          schema: db
  - name: units_edits
    using:
      foreign_key_constraint_on:
        column: first_harm_evt_inv_id
        table:
          name: units_edits
          schema: db
  - name: units_unifieds
    using:
      foreign_key_constraint_on:
        column: first_harm_evt_inv_id
        table:
          name: units_unified
          schema: db
