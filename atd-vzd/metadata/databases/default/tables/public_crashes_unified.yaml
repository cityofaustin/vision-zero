table:
  name: crashes_unified
  schema: public
object_relationships:
  - name: atd_txdot_location
    using:
      foreign_key_constraint_on: location_id
  - name: crashes_cri
    using:
      foreign_key_constraint_on: crash_id
array_relationships:
  - name: change_log_crashes_unifieds
    using:
      foreign_key_constraint_on:
        column: record_id
        table:
          name: change_log_crashes_unified
          schema: public
  - name: units_unifieds
    using:
      foreign_key_constraint_on:
        column: crash_id
        table:
          name: units_unified
          schema: public
