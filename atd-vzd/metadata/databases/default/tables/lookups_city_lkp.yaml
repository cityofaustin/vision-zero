table:
  name: city_lkp
  schema: lookups
array_relationships:
  - name: crashes_cris
    using:
      foreign_key_constraint_on:
        column: rpt_city_id
        table:
          name: crashes_cris
          schema: public
  - name: crashes_edits
    using:
      foreign_key_constraint_on:
        column: rpt_city_id
        table:
          name: crashes_edits
          schema: public
  - name: crashess
    using:
      foreign_key_constraint_on:
        column: rpt_city_id
        table:
          name: crashes
          schema: public
