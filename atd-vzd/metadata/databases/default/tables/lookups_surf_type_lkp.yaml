table:
  name: surf_type_lkp
  schema: lookups
array_relationships:
  - name: crashes_cris
    using:
      foreign_key_constraint_on:
        column: surf_type_id
        table:
          name: crashes_cris
          schema: public
  - name: crashes_edits
    using:
      foreign_key_constraint_on:
        column: surf_type_id
        table:
          name: crashes_edits
          schema: public
  - name: crashes_unifieds
    using:
      foreign_key_constraint_on:
        column: surf_type_id
        table:
          name: crashes_unified
          schema: public
