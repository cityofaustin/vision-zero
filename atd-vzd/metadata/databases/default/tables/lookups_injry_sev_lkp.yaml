table:
  name: injry_sev_lkp
  schema: lookups
array_relationships:
  - name: people_cris
    using:
      foreign_key_constraint_on:
        column: prsn_injry_sev_id
        table:
          name: people_cris
          schema: public
  - name: people_edits
    using:
      foreign_key_constraint_on:
        column: prsn_injry_sev_id
        table:
          name: people_edits
          schema: public
  - name: people_unifieds
    using:
      foreign_key_constraint_on:
        column: prsn_injry_sev_id
        table:
          name: people_unified
          schema: public
