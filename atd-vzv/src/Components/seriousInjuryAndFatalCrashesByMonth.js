import React from "react";

// Endpoint: https://data.austintexas.gov/resource/y2wy-tgr5.json
// Need to display line graph (x-axis is Jan-Dec, y-axis is count) of:
// 1. Year-to-date
// 2. Previous year

const SeriousInjuries = props => {
  return (
    <div>TODO Year-to-Date Serious Injuries - display sums as integers</div>
  );
};

export default SeriousInjuries;
