# Redirect to the index page of the SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# This rule handles the root path and redirects to /viewer/
[[redirects]]
  from = "/"
  to = "/viewer/"
  status = 302
