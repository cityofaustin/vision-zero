export const isDev = window.location.hostname === "localhost";

/**
 * Use this code to apply breaking changes to local storage settings.
 * when the code name is changed, user's will have their local storage
 * reset. the code name is the VZ release name
 */
export const appCodeName = "asclepias";
