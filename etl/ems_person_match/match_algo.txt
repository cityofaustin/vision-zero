
loop 1 — go through all PCRs and try to do the exact matches:
    if age/ethn/sex/travel_mode/pos_in_veh/inj_sev -> match 

loop 2 —  go through all PCRs again
    if age/ethn/sex/travel_mode/pos_in_veh -> match

loop 3 —  go through all PCRs again
    if age/ethn/sex/pos_in_veh -> match

loop 4 — go through all PCRs again
    if age/ethn/sex/fuzzy criteria -> match
