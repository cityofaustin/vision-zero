# docker-compose.override.yml
services:
  import:
    volumes:
      # mount local directory into working directory  
      - .:/app
      # mount local test_data directory where we expect the network directory
      - ./test_data:/mnt/vision_zero_cad
