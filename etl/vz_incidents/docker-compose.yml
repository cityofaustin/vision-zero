services:
  incidents:
    build: .
    env_file:
      - ./.env
    volumes:
      # mount local directory into working directory  
      - .:/app
    extra_hosts:
      - "host.docker.internal:host-gateway"
