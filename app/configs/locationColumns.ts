import { ColDataCardDef } from "@/types/types";
import { Location } from "@/types/locations";

export const locationColumns: ColDataCardDef<Location>[] = [
  {
    name: "location_id",
    label: "Location ID",
  },
];
